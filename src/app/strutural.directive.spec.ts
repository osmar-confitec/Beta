import { StruturalDirective } from './strutural.directive';

describe('StruturalDirective', () => {
  it('should create an instance', () => {
    const directive = new StruturalDirective();
    expect(directive).toBeTruthy();
  });
});
